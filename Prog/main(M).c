#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<ctype.h>

int main()
{
    while(1)
    {
        int t, j;
        scanf("%d %d", t, j);
        int p[t];
        char team[11];
        for(int i = 0; i < t; i++)
        {
            scanf("%s %d", team, p[i]);
        }

    }
    return 0;
}
